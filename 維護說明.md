# 「尋找調代課小幫手」維護說明

> 給接手維護的教學組長 / 行政同仁。
> 拿到新學期 PDF 後，照下面步驟做就能更新出新的查詢工具給全校用。

## 工具是做什麼的？

把全校老師的 PDF 課表轉成可查詢的網頁工具，老師打開就能：

1. 選擇自己 → 看自己的課表
2. 點請假時段 → 看誰可以代 / 調
3. 一鍵複製訊息 / 截圖 / 分享給對方

工具有兩種使用方式（詳見第三節）：

- **學校內網網址**（給老師日常用）：URL 固定、永遠是最新資料
- **單一 HTML 檔**（自己手機 / email / 離線雙擊）：一個檔案、不需網路

---

## 一、一次性的環境準備（第一次才需要）

### 1. 安裝 Python（建議 3.10 以上）

到 [python.org](https://www.python.org/downloads/) 下載安裝。**安裝時記得勾選 "Add Python to PATH"**。

### 2. 安裝唯一一個第三方套件

開啟 PowerShell，執行：

```powershell
pip install pdfplumber
```

> 這套件用來解析 PDF 表格。選單 [2] / [3]（微調資料、產單檔）不需要這個套件；只有選 [1]（重抽新 PDF）才需要。

### 3. 建立姓名筆劃對照表（只跑一次，之後無論換幾個學期都不用再跑）

雙擊 `更新課表.bat`，在選單外另開 PowerShell，執行：

```powershell
python scripts/build_strokes.py
```

這會下載 Unihan 資料庫（約 8 MB），生成 `assets/_strokes.json`。**除非這個檔被刪掉，否則之後永遠不需要再跑**。

---

## 二、日常操作：雙擊 更新課表.bat

所有操作都從這個入口進：

```
雙擊 更新課表.bat
```

進入後看到選單：

```
═══════════════════════════════════════════════
  尋找調代課小幫手 — 更新工具
  目前版本：114-2　已有版本：114-2
═══════════════════════════════════════════════
  [1] 新學期完整更新（PDF → data.js → 單檔 → 發布）
  [2] 微調 CSV 後重生 data.js 並發布（nginx 立即更新）
  [3] 只產單檔 HTML（自己手機 / email）
  [4] 分類確認表，人工檢查
  [5] 把母檔重新部署到 live/（改完 UI 後用）
  [H] 使用說明      [0] 離開
═══════════════════════════════════════════════
```

---

## 三、新學期更新步驟（每學期跑一次）

假設你拿到一份新的全校課表 PDF，整個更新**只要三步**：

### 步驟 1：把新 PDF 放進 `source/` 資料夾

把新檔案命名為 `全校課表.pdf`，放到 `source/` 資料夾裡（取代舊的同名檔）。

```
Switch_time/source/全校課表.pdf  ← 替換這個檔
```

> 注意：腳本只認這個固定檔名。如果你要保留舊檔，請先改名為 `全校課表_114-2.pdf` 之類，再放入新檔。

### 步驟 2：雙擊 更新課表.bat，選 [1]

選單選 **[1]**，輸入本學期版本字串（例如 `115-1`）：

```
請輸入資料版本（如 114-2）：115-1
```

> **版本字串**就是版本資料夾的名稱，也是網頁標題旁邊顯示的學期版本。
> 輸入後，進入查詢網頁時會在標題旁邊和確認視窗看到「資料版本 115-1」。

程式會自動依序完成：
- 從 PDF 抽取課表資料（CSV）
- 從 CSV 產出 data.js
- 產出單檔 HTML
- 發布到學校內網（live/）

### 步驟 3：人工確認分類（建議）

選單選 **[4]**，會產出 `versions/115-1/分類確認表.md`，**建議打開掃一眼**，特別注意這幾節：

| 章節 | 該檢查什麼 |
|------|------------|
| 1. 主授科目分布 | 各科人數合理嗎？（國/英/自/數/社/藝/體/特/其他） |
| 2. 每位老師主授科目 | 有沒有人科目被歸錯（特別是兼任老師） |
| 3. IB 教師清單 | 有沒有少 IB 老師、或誤把普通班老師當 IB |
| 4. 班級導師清單 | 對照學校導師名單是否吻合 |
| 9. 各科領域時間 | 領域時間的時段是否正確列出 |

**如果發現分類錯誤**，需要請有 Python 經驗的人調整 `scripts/extract_school.py` 內的分類規則，再重跑選單 [1]。

---

## 四、日常改幾筆資料（最常用）

改幾筆課表資料，**不需要重跑 PDF 抽取**：

1. 直接用 Excel 或文字編輯器開啟 `versions/<目前版本>/全校課表_長表.csv`，修改需要的欄位
2. 雙擊 `更新課表.bat` → 選單 **[2]**
3. 確認版本（直接 Enter 沿用目前版本），程式重生 data.js 並發布

**完成**。學校內網網址立即反映新資料，不需要動 HTML。

> 重要：改了 CSV 之後，**千萬不要再跑選單 [1]**，否則你的手動修改會被 PDF 重抽的結果覆蓋掉。

---

## 五、兩種成品說明

### (A) 單檔 HTML（自己手機 / email / 離線）

位置：`versions/<版本>/代課查詢_單檔.html`

- 一個獨立的 HTML 檔，所有資料內嵌在裡面
- 斷網也能用，可以雙擊直接在電腦上開
- 適合 email 給自己、存在手機上備用
- 選單 **[3]** 重新產出

### (B) live/ 內網網址（給全校老師用）

- nginx 服務 `live/` 資料夾，網址固定不變
- URL 始終是最新資料（每次選 [2] 或 [1] 發布後自動更新）
- 適合公告給全校老師用一個穩定連結
- 資料和 HTML 分開：可以只更新 data.js 而不動 HTML

> 原本的服務路徑（舊版的 `school_wide/`）已一次性切換為 `live/`，之後 URL 永遠穩定，不需再更動 nginx 設定。

---

## 六、改網頁外觀或文字

改 UI 外觀、按鈕文字、顏色等，**只需動 `代課查詢_發布.html` 這一個檔**：

1. 用文字編輯器（或 VSCode）開啟 `代課查詢_發布.html` 直接編輯
2. 可以直接用瀏覽器開這個檔預覽（沒有 data.js 時會顯示「請放 data.js」提示，屬正常現象）
3. 改完 → 選單 **[5]**（重新部署母檔到 live/）
4. 如果也要更新單檔 HTML → 再選 **[3]**

---

## 七、用 VSCode + Claude Code 對話完成

如果你不想記指令、不想開 Terminal，可以用 **VSCode** 加上 **Claude Code 擴充套件**直接用中文「講」給 Claude 聽，它會自己跑指令、改檔案、回報結果。

### 1. 一次性安裝（首次才需要）

1. 安裝 [Visual Studio Code](https://code.visualstudio.com/)（免費）
2. VSCode 左側 Extensions（四方塊圖示）搜尋 **Claude Code**，點 Install
3. 點側邊出現的 Claude 圖示 → 登入 Anthropic / Claude 帳號

> 學校若沒有公帳號，可在 [claude.ai](https://claude.ai) 自己申請。

### 2. 打開專案

VSCode → File → Open Folder → 選 `Switch_time/` 整個資料夾。

### 3. 直接跟 Claude 講

新學期 PDF 拖進 `source/`，命名為 `全校課表.pdf` 取代舊檔。然後在 Claude Code 對話框打：

```
新學期的全校課表 PDF 已經放好了，幫我跑完整流程：
抽取、產分類確認表，版本是 115-1
```

Claude 會依序執行選單 [1] 的所有步驟，並回報「IB 教師幾位、班級導師幾位」讓你確認。

### 4. 各種特殊狀況的範例對話

| 想做的事 | 直接打給 Claude |
|----------|------------------|
| 改某位老師的分類 | 「N60 這位老師主授被分成 X，幫我改成 Y」 |
| 改網頁標題、文案 | 「請把 XX 文字改成 YY」 |
| 看分類有沒有問題 | 「幫我列出最值得人工確認的老師有哪些、為什麼」 |
| 出錯訊息看不懂 | 把終端機紅字貼給它，問「這是什麼意思？」 |
| 想知道某個按鈕怎麼改 | 「按確認代課的綠色想換成藍色，幫我改」 |

### 5. 安全使用提醒

- Claude 改任何檔案**會先預覽 diff 給你看**，看不懂或不確定就按「拒絕」
- 不確定 Claude 剛剛做了什麼 → 直接問它「列出剛剛改了哪些檔案」
- **修改前先確認 git 狀態有 commit**：這樣即使改壞了也能還原

---

## 八、檔案地圖

### 你需要關心的檔案

| 路徑 | 角色 | 何時動到 |
|------|------|----------|
| `source/全校課表.pdf` | **輸入**：學期初拿到的原始課表 | 每學期換 |
| `代課查詢_發布.html` | **UI 母檔**：唯一需要手動維護 UI 的檔 | 改外觀 / 文字時 |
| `versions/<版本>/全校課表_長表.csv` | **課表真相**：所有產出都從這裡來 | 微調課表時 |
| `assets/_current_version.txt` | 目前版本記錄（選單自動維護） | 通常不手動動 |

### 自動生成的檔案（不要手動編輯，每次重跑都會被覆蓋）

| 路徑 | 內容 |
|------|------|
| `versions/<版本>/data.js` | 課表資料 JS，供網頁引用 |
| `versions/<版本>/代課查詢_單檔.html` | 資料內嵌的單檔成品 |
| `versions/<版本>/全校_extraction_report.md` | PDF 抽取過程的健檢報告 |
| `versions/<版本>/全校_領域時間.md` | 領域時間獨立列表 |
| `versions/<版本>/領域時間.json` | 結構化領域時間（供分類確認表用） |
| `versions/<版本>/分類確認表.md` | 人工確認用的分類表 |
| `live/index.html` | nginx 服務的 HTML（= 母檔複本） |
| `live/data.js` | nginx 服務的目前版本資料 |
| `assets/_strokes.json` | 姓名筆劃對照（build_strokes 產出） |

### 不需要動的腳本（通常不用管）

| 路徑 | 角色 |
|------|------|
| `scripts/paths.py` | 路徑集中管理（改版面才動） |
| `scripts/extract_school.py` | PDF 抽取 + 分類規則（規則微調時改） |
| `scripts/extract_v2.py` | PDF 解析底層（通常不動） |
| `scripts/build_data.py` | CSV → data.js（通常不動） |
| `scripts/build_single.py` | 母檔 + CSV → 單檔 HTML（通常不動） |
| `scripts/audit_categories.py` | 產分類確認表（通常不動） |
| `scripts/build_strokes.py` | 筆劃表（只跑一次） |
| `scripts/menu.py` | 選單主程式（通常不動） |
| `scripts/_legacy/` | 已退役封存的舊腳本（不用理） |

---

## 九、常見問題

### Q1：`pip install pdfplumber` 失敗怎麼辦？

通常是學校網路擋外部 PyPI。試：

```powershell
pip install pdfplumber -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### Q2：跑選單 [1] 出現「`source/全校課表.pdf` not found」

確認 PDF 檔名**完全是** `全校課表.pdf`（注意全形 / 半形、底線、空白等）。Windows 檔案總管預設隱藏副檔名，在「檢視」裡打開「副檔名」確認。

### Q3：網頁打開後選單空空的或出現「請放 data.js」

- **從瀏覽器直接開 `代課查詢_發布.html`**：這個檔本身不含資料，顯示提示屬正常現象。要看有資料的版本，請開 `versions/<版本>/代課查詢_單檔.html`，或從 live/ 的內網網址開。
- **從 live/ 網址開但空空的**：確認選單 [2] 或 [1] 已成功發布。

### Q4：某位老師被分類錯了

先看 `versions/<版本>/分類確認表.md` 的第 2 節確認誰錯。常見修法：

- **代號開頭族群錯**（例如 A 開頭老師被歸成自然）→ 改 `extract_school.py` 的 `PREFIX_TO_SUBJECT`
- **個別老師例外** → 在 `extract_school.py` 加個別覆蓋規則
- **IB / 普通班分錯** → 看那位老師的課程清單，調整判斷規則

改完規則後重跑選單 [1]。

### Q5：「資料版本」會顯示在哪裡？

版本字串（如 `114-2`）會出現在：

1. 網頁標題旁邊的小字
2. 進入頁面時的確認視窗

選單 [1] 或 [2] 輸入的版本字串，就是這兩處會顯示的內容。

### Q6：想同時保留舊學期和新學期的資料？

不同學期使用不同版本字串即可（如 `114-2` 和 `115-1`）。資料分別放在 `versions/114-2/` 和 `versions/115-1/`，互不影響。`live/` 只會是最近一次發布的版本。

---

## 十、求助

| 狀況 | 找誰 |
|------|------|
| 純粹照流程跑、報錯訊息看不懂 | 找學校資訊組 |
| 分類規則要大改（新增科目、新編號規則） | 需要改 Python 程式碼，找有 Python 經驗的人 |
| PDF 格式跟以前完全不同 | 同上，可能要重寫部分 `extract_school.py` |

設計沿革與技術細節在 [DEVLOG.md](DEVLOG.md)。技術文件在 [README.md](README.md)。
