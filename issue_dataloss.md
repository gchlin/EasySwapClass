# issue_dataloss：A11 老師在網頁選單消失的原因

> 資料版本：115-1　｜　調查日期：2026-09-03
> 相關檔案：[extract_school.py](scripts/extract_school.py)、[live/index.html](live/index.html)、[template/代課查詢_發布.html](template/代課查詢_發布.html)

> **狀態：全部修復完成（2026-09-04）。** 下拉選單已恢復 134 位老師，A11可正常選取；
> 領域時間也已依確認結果恢復成空堂（灰框標示）。規則決議見 [課程性質_待確認清單.md](課程性質_待確認清單.md)。

## 結論（一句話）

**PDF 沒有掉資料、CSV 沒有掉資料、data.js 也沒有掉資料。**
掉的是「下拉選單」：`populateTeacherDropdown()` 只列出 10 個科目白名單裡的老師，而 115-1 的課名改版讓A11被分到白名單外的「其他」科，於是整個人從選單消失。**同樣消失的共 7 位老師。**

---

## 資料鏈路逐段驗證

| 階段 | 檔案 | A11 資料 | 結果 |
|:--|:--|:--|:--:|
| 1. 原始 PDF | `source/全校課表.pdf` p.8 | 行政會議×2、藝能領域×2、CS、PE×2 = 7 筆 | ✅ |
| 2. 抽取 CSV | `versions/115-1/全校課表_長表.csv` | A11 共 7 筆，星期/節次/班級/教室全對 | ✅ |
| 3. 產生 data.js | `versions/115-1/data.js`、`live/data.js` | `__DATA__` 7 筆 + `__TEACHERS__` 1 筆 | ✅ |
| 4. 網頁下拉選單 | `live/index.html` `populateTeacherDropdown()` | **未列出** | ❌ |

PDF p.8 抽取結果（與 PDF 逐格比對一致）：

| 星期 | 節 | 課程 | 班級 | 教室 |
|:--:|:--:|:--|:--:|:--|
| 一 | 1 | 行政會議 | | |
| 一 | 2 | 行政會議 | | |
| 一 | 5 | PE | 114 | |
| 二 | 3 | 藝能領域 | | |
| 二 | 4 | 藝能領域 | | |
| 三 | 4 | CS | 114 | IB04 |
| 四 | 7 | PE | 114 | |

---

## 根因 A（顯示層）：下拉選單有科目白名單，「其他」不在裡面

[live/index.html:845](live/index.html#L845)

```js
const DROPDOWN_ORDER = ["自", "國", "英", "數", "社", "藝", "體", "特", "二外", "本土語"];
```

[live/index.html:988-1004](live/index.html#L988-L1004)

```js
const grouped = {};
for (const t of TEACHERS) { ...; (grouped[t.subject] ||= []).push(t); }
for (const sub of DROPDOWN_ORDER) {      // ← 只跑白名單
  const list = grouped[sub];
  if (!list || list.length === 0) continue;
  ...
}
```

`grouped["其他"]` 從來沒有機會被塞進 `<select>`。**沒有錯誤訊息、沒有 fallback、沒有計數檢查**，所以是靜默掉資料。

`__TEACHERS__` 共 134 人，選單只列出 **127 人**。

### 一併消失的 7 位老師

| 代碼 | PDF 職稱 | 114-2 主授科目 | 115-1 主授科目 |
|:--|:--|:--:|:--:|
| A04 | 教師(健) | 體 | **其他** |
| A05 | 秘書(體) | 體 | **其他** |
| A11 | 國際交流處主任(體) | 體 | **其他** |
| A15 | 315 導師(健)CAS | 社 | **其他** |
| A23 | 教師(科技) | 藝 | **其他** |
| A62 | 兼課師(體) | （114-2 無此人） | **其他** |
| A63 | 兼課師(體) | （114-2 無此人） | **其他** |

114-2 的「其他」是 **0 人**，115-1 變成 **7 人** → 這是這學期新產生的回歸。

---

## 根因 B（資料層）：115-1 學校改了課名，分類器關鍵字沒跟上

`classify_course_subject()`（[extract_school.py:49-113](scripts/extract_school.py#L49-L113)）用「課名關鍵字投票」決定主授科目，彈性課回傳 `None`（不投票），無法判定就回傳 `"其他"`。

115-1 的 PDF 改了三組課名，關鍵字全部落空：

| 114-2 課名 | 115-1 課名 | 彈性課關鍵字 | 115-1 判定 |
|:--|:--|:--|:--|
| 行政會**報**（60 筆） | 行政會**議**（60 筆） | 清單只有 `"行政會報"` | ❌ 落到「其他」|
| 領域時間（226 筆） | 藝能領域／社會領域／國文領域／自然領域／英文領域／數學領域／二外領域（216 筆） | 清單只有 `"領域時間"`、`"領域課程"` | ❌ 部分落到「其他」，部分被誤判成實科 |
| 團體活動 | 團體活動**時間** | `"團體活動"` 是子字串 | ✅ 仍正常 |

「XX領域」還造成**不一致的誤判**（更難察覺）：

- `英文領域` → 命中 `"英文"` → 投給 **英**
- `數學領域` → 命中 `"數學"` → 投給 **數**
- `國文領域`／`社會領域`／`自然領域`／`藝能領域`／`二外領域` → 沒命中任何關鍵字 → 投給 **其他**

也就是說，同一類「領域時間」課，有的變成有效科目票、有的變成「其他」票。

### A11 的投票結果

| 課名 | 筆數 | 分類 |
|:--|:--:|:--|
| 行政會議 | 2 | 其他 |
| 藝能領域 | 2 | 其他 |
| CS | 1 | 其他 |
| PE | 2 | 體 |

`其他 5 : 體 2` → 主授科目 = **其他**。
114-2 時她只有 `行政會報×2`（判為彈性、不投票）+ `PE×2` → `體 2 : 0` → 主授科目 = **體**，所以去年正常顯示。

代號 prefix 強制覆蓋（[extract_school.py:316-323](scripts/extract_school.py#L316-L323)）救不了她：`PREFIX_TO_SUBJECT` 只涵蓋 `C/E/F/G/M/N/S`，**`A` 系刻意排除**（A 系混合體/藝/社），所以 A 系老師完全靠課名投票。7 位消失的老師**全部是 A 系**，不是巧合。

---

## 連帶災情（同一個課名改版造成，順便記錄）

1. **領域時間排除失效**
   [extract_school.py:273](scripts/extract_school.py#L273) 用 `if course == "領域時間"` 嚴格相符才把該格排除出 CSV 並另存 `領域時間.json`。
   - 114-2：排除 **226** 筆 → 這些格子在網頁上是**空的（可調課）**
   - 115-1：排除 **1** 筆 → 216 筆「XX領域」留在 CSV → 網頁上變成**有課（不可調課）**
   影響 **108 位老師**、每人固定 2 節（216 格）。這會大幅減少可調課時段，且未經確認。
2. **`全校_領域時間.md` 部門共同時段全變「（無）」** — 因為只剩 1 筆資料。
3. **藍框「只能代課」規則失效**
   [live/index.html:944-946](live/index.html#L944-L946) `isSubOnlyCourse()` 判斷 `name.includes("行政會報")`，115-1 課名是「行政會議」→ 60 筆行政會議格子**變成可以調課**。
4. **紫框連堂白名單失效**
   [live/index.html:939-942](live/index.html#L939-L942) `isInquiryCourse()` 白名單含 `"領域課程"`，115-1 已無此課名。
5. `versions/115-1/` 沒有產出 `分類確認表.md`（114-2 有），所以上述分類異常沒有在人工確認環節被攔下來。

---

## 修復記錄（2026-09-03 已完成）

### ✅ P0-1　顯示層加保險：選單不再靜默丟人
[template/代課查詢_發布.html](template/代課查詢_發布.html) 新增 `dropdownSubjects()`：白名單 `DROPDOWN_ORDER` 打頭，**資料裡有但不在白名單的科目一律補列在最後**，並在 console 印出警告。
`populateTeacherDropdown()` 與 `buildSubjectButtons()` 都改用它，所以不論分類器怎麼判，老師都不會再整個人消失。

### ✅ P0-2　資料層：課名判斷改成規則式
[extract_school.py](scripts/extract_school.py) `classify_course_subject()` 在 flex 清單前加上規則式比對：

```python
if name.startswith("行政會") or name.endswith("領域"):
    return None
```

擺在 `國/英/數` 判斷之前，順手修掉「`英文領域`被投給英、`數學領域`被投給數，其餘領域課投給其他」的不一致。
flex 完整課名清單也補上 `"行政會議"`（保留 `"行政會報"` 讓舊版 CSV 仍可重跑）。

### ✅ P1　A/L 系改用 PDF 職稱括號定主授科目
新增 `POST_TAG_TO_SUBJECT` 對照表與 `post_subject()`：`(健)(體)(防護員)(國防)→體`、`(科技)(資)(家)(音)(美)(視覺)(表藝)→藝`、`(歷)(地)(公)(生涯)→社`…

優先序：**代號 prefix（C/E/F/G/M/N/S）＞ 職稱括號 ＞ 課名投票 ＞「其他」**。

職稱括號放在 prefix 之下，所以 F16 `教師(生涯)` 仍維持 `特`（F 系強制覆蓋不變）。
對 134 位老師做過全量比對：職稱括號的判定與原本課名投票**在 124 位上完全一致**，其餘 10 位中 7 位就是這次消失的老師（全部判對），2 位（A07「213 導師+體召 CAS」、A32「教練」）職稱沒有科別括號 → 自動退回課名投票，結果不變。

### ✅ P2　流程層自動檢查
`extract_school.py` 在寫 CSV 前檢查「主授科目 ∉ 網頁科目清單」的老師，有的話逐位印出代碼、姓名、職務字串。本次重跑**無警告**。

順帶修掉 [tests/triangle.test.js:16](tests/triangle.test.js#L16) 的失效路徑（仍指向已改名的 `代課查詢_發布v2.html`），測試恢復可執行，27 項全過。

### ✅ P0-3　領域時間格子（2026-09-04 補做，已確認 Q2）
`extract_school.py` 的排除條件放寬成「課名以『領域』結尾」（排除「領域課程」），
217 格領域時間重新排除出 CSV → 網頁上恢復成空堂，另用灰色框線標示是哪一科的領域時間。
行政會議依 Q1 確認為「可調可代 + ⚠ 提示」，不恢復藍框；團體活動時間依 Q3 改為藍框。

### ✅ 補漏：只排領域時間的老師會整個消失
排除領域時間後發現 **N28**（教師(物)）全學期只有「自然領域」兩格，CSV 裡變成零筆，
而 `build_data.py` 的教師名冊是**從 CSV 反推**的 → 他從 134 位掉到 133 位。跟A11是同一類 bug。

修法：`extract_school.py` 另外輸出 `versions/<版本>/teachers.json`（名冊來源＝**PDF 每頁表頭**，
不是 CSV 反推），`build_data.py` / `build_single.py` 有這個檔就以它為準，沒有則沿用舊行為（相容 114-2）。

### 實際變更（CSV 逐格 diff）
2035 筆資料**只有「主授科目」欄變動、只影響 7 位老師**，星期/節次/課程/班級/教室全部一字未動：

| 代碼 | 修復前 | 修復後 | 依據 |
|:--|:--:|:--:|:--|
| A04 | 其他 | 體 | 教師(健) |
| A05 | 其他 | 體 | 秘書(體) |
| A11 | 其他 | 體 | 國際交流處主任(體) |
| A15 | 其他 | 體 | 315 導師(健) |
| A23 | 其他 | 藝 | 教師(科技) |
| A62 | 其他 | 體 | 兼課師(體) |
| A63 | 其他 | 體 | 兼課師(體) |

> ⚠ A15 在 114-2 是「社」，現在依職稱 `315 導師(健)` 判為「體」。學校自己標的是 (健)，但這位老師也教社會心理學概論 —— **這位請你確認要歸哪一科**。

---

## 驗證結果

```powershell
python scripts/extract_school.py   --version 115-1   # 無「主授科目不在清單」警告
python scripts/build_data.py       --version 115-1
python scripts/build_single.py     --version 115-1
python scripts/audit_categories.py --version 115-1   # 補回 115-1 缺的分類確認表.md
python -c "import sys;sys.path.insert(0,'scripts');import menu;menu.publish('115-1')"
node tests/triangle.test.js     # 27 PASS
node tests/rules.test.js        # 18 PASS（新增）
```

| 檢查項 | 結果 |
|:--|:--|
| CSV 主授科目集合 | 已無「其他」 |
| `__TEACHERS__` 人數 | 134（修復前 127 可選；中途一度掉成 133，見上） |
| 下拉選單可列出人數 | 134 |
| A11 | 在「體」分組，5 筆課 + 2 格藝能領域（灰框空堂） |
| N28 | 在「自」分組，0 筆課 + 2 格自然領域（灰框空堂） |
| `__DATA__` 筆數 | 1819（原 2035 − 216 格領域時間） |
| `__RYU__` 筆數 | 217 |
| 殘留「XX領域」課程 | 0 |
| E62 `201 Wri` | 已拆成 課名 `Wri` / 班級 `201` |
| 內嵌 JS 語法 | `node --check` 通過 |
| 測試 | 27 + 18 = 45 PASS |

`live/` 已重新發布，nginx 服務的就是修好的版本。
