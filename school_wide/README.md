# Local school-wide outputs

This directory is for private school-wide CSV, Markdown, JSON, and generated HTML outputs.

Files in this directory are intentionally ignored by Git because they can contain timetable data.

