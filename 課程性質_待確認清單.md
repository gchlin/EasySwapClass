# 課程性質 規則確認記錄

> 資料版本：115-1　｜　確認日期：2026-09-04　｜　狀態：**Q1～Q6 已確認並實作完成**
> 相關：[issue_dataloss.md](issue_dataloss.md)
> 尚待確認：**只剩附錄 B**（其他掉進「其他」科的課名）。

## 一、已確認的規則（已進程式）

| 項目 | 一般課程 | 行政會議 | XX領域（領域時間） | 團體活動時間 |
|:--|:--|:--|:--|:--|
| 115-1 規模 | — | 60 格 / 30 人 | 217 格 / 108 人 | 48 格 / 48 人 |
| 固定時段 | 各自不同 | 一1、一2 | 依科別（藝二3-4、社三3-4、國一5-6、自四5-6、英二5-6、數三5-6、二外二5-6） | 五5、五6 |
| 在 `__DATA__` 裡 | ✅ | ✅ | ❌ **排除（視同空堂）** | ✅ |
| 網頁格子 | 有課 | 有課 | **空堂＋灰框標示課名** | 有課 |
| 可當「我請假的那節」 | ✅ | ✅ | 不適用（空堂） | ✅ |
| 可當「對調目標時段」 | ✅ | ✅ | ✅（空堂可被調進來） | ❌ |
| 可被人代課 | ✅ | ✅ | 不適用 | ✅ |
| 框線 | 無 | 無 | **灰框** | **藍框** |
| ⚠ 提示 | 無 | **有** | 無 | **有** |
| 算主授科目票 | ✅ | ❌ | ❌ | ❌ |

### Q1　行政會議 → **可調可代 + ⚠ 提示**
不做藍框。格子上顯示 ⚠，點選後面板出現：
> ⚠️ 這是行政會議／團體活動時間，不是一般授課。原則上可以調課或代課，但請自行跟相關單位確認。

判斷式改為含「行政會」，所以舊課名「行政會報」一樣命中（Q6）。

### Q2　XX領域（領域時間）→ **視同空堂，只用灰色框線標示**
- `extract_school.py` 把「以『領域』結尾」的格子整格排除出 CSV（「領域課程」是真的有學生的課，不在此列），另存 `領域時間.json`。
- `build_data.py` / `build_single.py` 把它輸出成 `window.__RYU__`，**只給網頁畫灰框用**。
- 所有調代課邏輯看到的都是空堂：別人可以調進來，自己請假時這格不用處理。
- 網頁該格顯示灰框 + 淡色課名（如「藝能領域」），圖例出現「灰框表示領域時間（視同空堂，可被調進來）」。

> 先前提過的顧慮（同科老師領域時間全變空堂 → 代課媒合會推薦一整科，但那時全科都在開會）**目前沒有另外處理**。如果實際用起來會誤導，再告訴我加「該科領域時間，全科不可用」的標示。

### Q3　團體活動時間 → **藍框，只能代課不能調課**
判斷式含「團體活動」，舊課名「團體活動」一樣命中（Q6）。同時也吃 ⚠ 提示。

### Q4　藍框只擋單邊 → **維持現況**
只檢查「你請假的那一節」；對方的多元／自主／團體活動格子仍可列為對調目標，由對方自行判斷。

### Q5　三角調中繼 → **可以，但加 ⚠ 提示**
不限制 B 那一節的課程性質。行政會議／團體活動時間的格子帶 ⚠，提醒使用者這不是一般授課。

### Q6　判斷方式 → **規則式比對**
| 類別 | 規則 | 命中的課名 |
|:--|:--|:--|
| 領域時間 | 以「領域」結尾（排除「領域課程」） | 領域時間、藝能領域、社會領域、國文領域、自然領域、英文領域、數學領域、二外領域 |
| 行政會議 | 以「行政會」開頭 / 含「行政會」 | 行政會報、行政會議 |
| 團體活動 | 含「團體活動」 | 團體活動、團體活動時間 |

下學期學校再改課名（只要沒改掉這幾個詞根）不用動程式。

## 二、順帶處理的兩件事

### A15 → **體**（已確認）
依 PDF 職稱 `315 導師(健)` 判為「體」，非 114-2 的「社」。

### `201 Wri` / `101 Wri` / `301 Wri`（E62）→ **自動拆成 班級 + 課名**
PDF 原始格子就是單行 `201 Wri`（排課系統沒把班級另起一行）。
`extract_v2.parse_cell()` 加上：班級欄為空且課名以 3 位數班級代碼開頭時，拆成 課名 `Wri` + 班級 `201`。
這 3 格現在能被「同班老師」媒合抓到 101/201/301 了。

> 還是不知道 `WG`／`OH`／`TSP`／`Wri` 各是什麼課。若你問到 E62 這位老師，告訴我要不要把它們歸科或改名。

## 三、自動化測試

新增 [tests/rules.test.js](tests/rules.test.js)，把上面每條規則都寫成斷言（18 項），
連同既有的 [tests/triangle.test.js](tests/triangle.test.js)（27 項）一起跑：

```powershell
node tests/rules.test.js
node tests/triangle.test.js
```

## 附錄 B：其他掉進「其他」科的課名（⏸ 仍待你確認）

這些課名沒被任何科目關鍵字命中，目前投給「其他」。多數老師還有其他課撐住科別所以不受影響，但會影響「同科優先」的排序準確度。**建議應歸科別**是我的推測，請確認：

| 課名 | 筆數 | 目前 | 建議應歸 | 確認 |
|:--|:--:|:--:|:--:|:--:|
| 現代社會與經濟 | 21 | 其他 | 社 | □ |
| 社會環境議題 | 21 | 其他 | 社 | □ |
| 世界社會環境議題與國際關 | 2 | 其他 | 社 | □ |
| 健康與護理 | 13 | 其他 | 體（健護） | □ |
| 全民國防教育 | 13 | 其他 | 體 | □ |
| 生活科技 | 12 | 其他 | 藝（生科） | □ |
| 國文充補 | 12 | 其他 | 國 | □ |
| 基本設計 | 7 | 其他 | 藝 | □ |
| 工程設計專題 | 6 | 其他 | 藝（生科） | □ |
| SEHS SL | 6 | 其他 | 體 | □ |
| 臺灣手語 | 4 | 其他 | 本土語？二外？ | □ |
| CS / Computer SciSL / Computer SciHL | 8 | 其他 | 藝（資訊） | □ |
| ESS SL | 3 | 其他 | 自 | □ |
| WG | 6 | 其他 | ？ | □ |
| CP | 3 | 其他 | 特？ | □ |
| OH / TSP | 4 | 其他 | ？ | □ |
| 創新生活與家庭 | 2 | 其他 | 藝（家政） | □ |
| AI程式設計應用與開發(一) | 2 | 其他 | 藝（資訊） | □ |
| 走讀臺灣 | 2 | 其他 | 社 | □ |
| 101/201/301 Wri | 3 | 其他 | 英 | □ |
