# Dev Log — 全校代課查詢網頁

時間軸與設計決策記錄。供其他工程師了解需求演進與我們做出的取捨。

## 起點

自然科 16 位老師的代課查詢網頁（`代課查詢.html`）已經穩定運作。資料源是手動校對過的 `課表_長表.csv`（229 筆），UI 將候選代課老師依「主授科目」（物/化/生/地科）分組顯示。

## 階段一：擴大到全校 130 位老師

### 需求

擴大到全校 130 位老師。先從 PDF 抽出 1816 筆課程資料到 `全校課表_長表.csv`，需要新增「教師類別」欄位區分 IB 教師與普通班教師。

### 第一版規則：含英文字母 = IB

> 找出課程名稱有用英文寫的，這些教師定義為 IB 教師，其餘普通班。

直觀但**誤判嚴重**：
- `英語文 B` 的 B 是普通班分組標記 → 14 位英文老師被錯標為 IB
- `數學 A` / `數學 B` → 5 位數學老師
- `日語 A` / `日語 B` → 2 位
- `Fun 影片高一多元`、`EWANT 數位學習` → 3 位 MOOC/多元課老師
- 其他 CSV 解析瑕疵造成的偽英文 → 3 位

### 第二版：嚴格 IB 字串 + 教室證據

改用 IB 慣例字串列表精準匹配：
```
SL/HL/TOK/CAS/IPSS/ChineseA/EnglishA/EnglishB/Mandarin/VA-/MathAA/MathAI/ChiBS
```

對於課名沒明確 IB 標記但以教室代碼佐證為 IB 的 10 位老師（在 IB04~IB14、114A-116A 教室授課），手動列入 IB 教師。

最終 35 位 IB 教師、95 位普通班教師。

## 階段二：發現 PDF 解析瑕疵

校對時發現 12 筆奇怪的列：
```
A18,（老師A18）,2,5,圖書館自高二自主S1304(Se,,
C10,（老師C10）,2,5,高二自主高二自主S1303(Li,,
```

教室代碼 `S1xxx(...)` 被吞進課程名稱欄位。追到根因：`extract_v2.py` 的 `is_room_like()` 只認 `T/A/IB` 開頭的教室碼，沒處理 `S` 開頭。

S 開頭教室都是「自主學習教室／圖書館」性質，剛好就是 自主/充實/多元 這類沒指定班級的彈性課程在用。

修正：
1. 改 `is_room_like()` 加 `S` 開頭判斷（未來重抽不會再出問題）
2. 一次性修正現有 12 筆 CSV 列（拆出教室碼、補上右括號）

## 階段三：資料夾重組

```
課表分析/
├── README.md
├── scripts/         # 所有 .py
├── source/          # 原始 PDF
├── natural_science/ # 自然科 CSV/MD/HTML
└── school_wide/     # 全校 CSV/MD/HTML
```

把硬編路徑改成 `Path(__file__).parent.parent / "..."`，腳本可以從專案根目錄執行。

## 階段四：建構全校代課查詢 v1

新增 `build_web_school.py`，沿用自然科版的 UI/JS 結構，但：
- 候選分組改用 `教師類別`（IB / 普通班）
- 130 人選單加上 `<datalist>` 搜尋輔助

跑出來的網頁能用，但**設計有問題**：UI 上掛著「IB 教師」「普通班教師」標籤。

## 階段五：UX 大改 — 去階級化、改用主授科目分組

### 使用者回饋（關鍵）

> 普通班教師、IB教師，這樣的分類總是不會出現在介面上（很多老師會誤會有階級之分）

階級觀感是真實顧慮。即使內部資料模型分 IB/普通，UI 不能露出。

### 改設計

| 項目 | 改前 | 改後 |
|------|------|------|
| 候選主分組 | 同類別 / 其他類別 | 同科 / 其他科 |
| 「其他科」拆細 | 無 | 拆 國/英/自/數/社/其他 |
| 老師標籤 | `（IB教師）` | `（科目）` |
| IB 課程例外 | 無 | IB同科 → IB其他科 → 同科 → 其他科 四階優先 |
| 教師代號搜尋 | 無 | datalist 同時收姓名與代號 |
| 同組內排序 | 無特定 | 教師代號由小到大 |

### 衍生的資料工程問題

「主授科目」CSV 沒這欄，要從每位老師的課程自動推。寫了 `classify_course_subject()` 規則：
- 特殊 IB 先判斷（TOKCh→國、TOKEng→英）
- 社會優先（避免 `歷史學探究` 被「探究」抓走歸到自）
- 篩掉彈性課（行政/團體/自主/多元/充實/深廣/領域時間/Adm sup/Eng sup/CT/TOK/CAS/INS）
- 再依字串特徵分國/英/數/自/社
- 沒命中 → 其他

每位老師主授 = 計入課程中出現次數最高的類別。少數曖昧案例（M36：5 自 vs 5 數打平）就接受演算法的選擇，使用者可以單筆改 CSV 覆蓋。

### IB 課程偵測

JS 端用 `IB_PATTERNS` 字串陣列做 substring 比對，跟 CSV 教師類別不重複。決定該節要不要走「IB 4 階優先順序」是看那節的課名，不是看老師是否為 IB。

## 階段六：工作流程定稿

CSV 是**單一真相來源**，HTML 永遠由 build script 重生。但要小心：

- 改 CSV → 跑 `build_web_school.py`（安全）
- 改 CSV → 跑 `extract_school.py`（**手動修正會被覆蓋**）

「資料型」修改（單筆教師主授錯）→ 改 CSV。
「規則型」修改（要把 BMSL 從社改到自）→ 改 `extract_school.py` 的分類器，重抽。

## 階段七：同班 / 導師優先

### 需求

> 預設候選順序：同科 → 同班級 → 其他科。同班任課老師中，導師排最前面（提醒使用者：調課優先找導師）。
> IB 課程：IB同科 → IB同班 → IB其他科 → 同科 → 其他科。

### 實作

- `extract_school.py` 新增 `extract_homeroom_class()`，從 PDF header 抽 `(\d{3}[A-Z]?)\s*導師` 模式 → 48 位班級導師偵測成功
- F09（特教導師）無班號，正確回空字串
- CSV 加 `導師班級` 欄
- `build_web_school.py`:
  - 預先建立 `KLASS_TO_TEACHERS` 索引（O(N) once）以判斷「同班候選」
  - 候選邏輯改 3 階／5 階互斥分群
  - `sortCandidates()` 一律把該班導師排第一
  - 候選按鈕若是該班導師，姓名後加紅色「（導師）」字樣

### 為什麼導師標記要紅字

使用者反映：**調課實際慣例就是優先找導師**。要讓導師身分一眼可辨，紅字 + 「（導師）」是教育界既定的視覺語言。

## 階段八：科目細分與分類審查表

### 需求

> 科目「其他」太籠統，應再細分：音樂/美術/藝術/生科/資訊 → 藝；體育 → 體；日/德/法/西 → 二外。
> 我們有許多歸類（IB 老師、IB 課程、領域時間、自主課程、導師…），輸出一份 md 讓我確認。

### 實作

`classify_course_subject()` 從 6 類擴充為 9 類：
- **藝**：音樂、美術、藝術生活、新媒體藝、生科、資訊科技、VA/PA/AW（7 位）
- **體**：體育、PE、健護、運動、專項技術／體能、SEHSSL（13 位）
- **二外**：日語、德語、法語、西班、西語、韓語（9 位）

「二外」檢查移到 flex 之前，避免 `韓語高一多元` 這類僅有彈性課的純語言老師被吞到「其他」（例如 L23）。

### 分類審查表

新增 `scripts/audit_categories.py` → `school_wide/分類確認表.md`，內容含：
1. 主授科目分布（9 類）
2. 每位老師主授／IB／導師班一覽
3. IB 教師清單（35 位）
4. 班級導師清單（48 位，依班號排）
5. IB 課程清單（matching 13 個 IB pattern）
6. 含「領域」課程（區分領域時間 vs 領域課程）
7. 自主／自學相關課程
8. **各主授科目對應的實際課程列表**（讓人一眼看出分類器把哪些課歸到哪一類）

### 9 類最終分布

| 國 | 英 | 數 | 自 | 社 | 藝 | 體 | 二外 | 其他 |
|----|----|----|----|----|----|----|------|------|
| 23 | 19 | 15 | 17 | 22 | 7  | 13 | 9    | 5    |

剩 5 位「其他」是真正歸不進任何特定領域的：A08（家政）、A38（安全教育）、F06（CP+生命教育）、F07（純彈性課）、G22（全民國防）。如果之後想再細分，分類器再擴展即可。

## 階段九：代號 prefix 強制覆蓋

### 需求

> 原則上同代號就會是同科目族群 → 全部都強制用代號歸類。

驗證後確認 6 個例外都是 IB 跨領域老師（如 E41 教 BMHL/BMSL 但代號 E）或單一例外（F06、G22）。學校內部的代號 = 行政組織分類，比實際授課科目更貼近「找哪位同事代課」的真實邏輯。

### 實作

`extract_school.py` 在 course-based 分類算完後，疊一層 prefix override：

```python
PREFIX_TO_SUBJECT = {
    "C": "國", "E": "英", "F": "社", "G": "體",
    "L": "特", "M": "數", "N": "自", "S": "社",
}
# A 例外：沿用課程式分類（A 內部真的混合 體/藝/家政/資訊/生科）
```

### 結果

| C 17 | E 20 | F 5 | G 1 | L 15 | M 16 | N 16 | S 17 |
|------|------|-----|-----|------|------|------|------|
| 國 | 英 | 社 | 體 | 特 | 數 | 自 | 社 |

A 族群維持課程分類：體 14 / 藝 8 / 社 1（A15 IB Psy 老師）。

「二外」與「其他」類別在覆蓋後實際上沒有教師（二外 9 位都歸 L→特；其他 3 位都被 prefix 收編）— 在 UI 中仍保留為候選類別位置，留給未來可能的單筆覆蓋。

### 改動 vs course-based 的差異（10 位老師被勘正）

| 代號 | 原 (course) | 新 (prefix) |
|---|---|---|
| E26 | 其他 | 英 |
| E41 | 社 | 英 |
| F06 | 其他 | 社 |
| F07 | 其他 | 社 |
| G22 | 其他 | 體 |
| M36 | 自 | 數 |
| L (15 位) | 二外/國/其他 | 特 |

對代課用途：找代課時優先看的是「這位老師是哪個辦公室／組別的同事」，而非「他真實授課科目是什麼」。學校的代號制度本身就是組織歸屬，prefix override 直接對齊。

## 階段十：細科目子排序

### 需求

> 有時需要找特定細科（譬如物理化學都是自然，但有時要找物理）。社會科同理。

### 實作

CSV 加 `細科目` 欄。從每位老師的課程推：物/化/生/地科、史/地理/公民/法律/心理/商管、音樂/美術/家政/視覺藝術/表演藝術/資訊/生科、體育/健護/安全教育/全民國防、日/德/法/西/韓/客語/閩南語/原住民語、國文/英文/數學…約 30 種。

UI 在「同科」群組內子排序：**該班導師 → 同細科 → 其他（按代號）**。
- 候選人按鈕加 `★` 綠字標記同細科。
- 括弧內顯示「主授／細科目」（細科與主授相同則只顯示主授，避免冗餘）。

### 結果（自然科 N 族群）

| N01 | N02 | N03 | N04 | N06 | N11 | N38 | N45 | N49 | N51 | N52 | N59 | N60 | N63 | N68 | N69 |
|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|-----|
| 物 | 化 | 生 | 地科 | 化 | 生 | 物 | 化 | 物 | 生 | 地科 | 生 | 物 | 物 | 化 | 化 |

物理老師請假找代課時，同樣是物理的 N01/N38/N49/N60/N63 會在 N 族群內排在最前面，化/生/地科老師排後面。

### Trade-off

M36（IB 雙主修 物理+數學）主授=數（prefix override），細科=物（按課程主流）。在 N 族群（物理）的 leave slot 中**不會**被列為候選 — 因為主授已固定在 M。屬於 prefix override 設計的可預期取捨。

## 階段十一：姓名筆劃排序

### 需求

> 群組內排序由「教師代號」改成「姓名筆劃 小→大」。對使用者來說，找老師看姓名比看代號直覺。

### 取捨：套件選擇

兩個方案：
- A. 用 Python 套件算筆劃，build 時生成已排序資料 → HTML 端零變動
- B. JS 端 `localeCompare`／Unicode 點位排序 → 不裝套件，但**不是真的筆劃序**（部首排序與筆劃排序差很多）

選 A。但 `cnchar` 是 JS 套件，Python 沒對應。試了 `hanzipy`（recursive 部首拆解，不對）、`cjklib2`（不存在）、`unihan-etl`（只是下載工具）。

最後直接從 **Unicode Unihan 資料庫**（https://www.unicode.org/Public/UCD/latest/ucd/Unihan.zip）抽 `kTotalStrokes` 欄位。寫 `scripts/build_strokes.py` 下載 + 解析 + 快取，產出 `school_wide/_strokes.json`。230 個漢字全部抽到。

### 實作

`build_web_school.py` 載入 `_strokes.json`，計算每位老師全名筆劃，寫進 `TEACHERS[i].strokes`。
`sortCandidates()` 排序鍵改成：

```
該班導師 → 同細科 → 姓名筆劃 (小→大) → 教師代號 (deterministic tiebreak)
```

外籍老師（如 E26/E41，以英文名為名）給 999 筆劃 → 排到該群組最後。

### 一個小瑕疵

陳 字 Unihan kTotalStrokes = 10（簡寫 convention），繁體字典算 11。差 1 筆不影響整體排序順位。如果要嚴格繁體字典筆劃，再寫個 override dict。

### N 族群按筆劃排序結果

16 位老師依全名筆劃由小到大排序（代碼：N01/N02/N03/N04/N06/N11/N38/N45/N49/N51/N52/N59/N60/N63/N68/N69）。實際排序需執行 `build_web_school.py` 後查看，結果含識別資訊不在此記錄。

物理老師找代課（細科=物優先）→ N01/N38/N49/N60/N63 依筆劃排列在 N 族群最前。

## 重要的設計取捨

1. **資料嵌入 HTML 而非 fetch CSV**：要讓 HTML 雙擊離線開（`file://` 下 fetch 被 CORS 擋）
2. **自然科與全校兩套 build pipeline**：自然科有人工校對的 gold standard 與專屬視圖；全校以 pdfplumber 直接輸出，工具集較精簡
3. **CSV 含三個衍生欄位（主授科目／教師類別／導師班級）**：denormalized 但每筆查詢不需要重算；資料嚴格由 `extract_school.py` 重生時保持一致
4. **UI 隱藏 IB 標籤**：避免階級觀感；內部資料模型保留以支援 IB 課程的優先順序例外
5. **同班候選只在非同科時顯示**：避免重複（同科同班會在 `同科` 群組中以「（導師）」標記呈現）

## 未來改進可能

待辦清單已搬移至 [TODO.md](TODO.md)（含「降低介面文字量」討論記錄）。

---

## 階段十二：v2 架構重構（2026-06）

### 核心決策：資料 / UI 分離

舊架構（`build_web_school.py` 整頁生成）的問題：每次 UI 改版後，HTML 樣板與 Python 程式碼需要同步維護兩份；任何一方有變動就容易漂移。

v2 改為：

- **UI 母檔**（`代課查詢_發布.html`）：唯一手改 UI 的地方，可以用瀏覽器直接預覽（無 data.js 時顯示「請放 data.js」提示）。內含 `<script src="data.js">` 外部引用，標題與確認視窗的版本號由 `window.__DATA_VERSION__` 動態注入。
- **資料 JS**（`data.js`）：由 `build_data.py` 從 CSV 產出，內含 `window.__DATA_VERSION__`、`window.__DATA__`、`window.__TEACHERS__` 三個全域變數。

更新資料只需重生 data.js，HTML 一字不改。改 UI 只動母檔，不需要把程式碼同步到生成器。

### 雙路徑發布設計

同時支援兩種用途，均為有效發布路徑：

- **(A) 單檔 HTML**（`versions/<版本>/代課查詢_單檔.html`）：`build_single.py` 把母檔的 `<script src="data.js">` 替換成內嵌資料，產出自我包含的單檔。適合自己手機 / email / 離線雙擊。
- **(B) live/**：`menu.py` 負責發布，把最新 data.js 複製到 live/，同時複製母檔為 live/index.html。nginx 服務這個固定目錄，URL 永遠穩定。

舊架構以 `school_wide/` 作為 nginx 服務目錄，已一次性切換為 `live/`；不需改 nginx.conf，只是 web root 下的路徑變動。

### 依版本歸檔 + 穩定 live

產出依版本歸檔在 `versions/<版本>/`，live/ 只放目前發布版。決策：

- 同一版本重跑 = 覆蓋（重抽 CSV 前加確認提示）。
- 不做「(1)(2)(3)」序號編號：版本字串本身即版本識別（`114-2`、`115-1`）；要保留舊版本就用不同版本字串，versions/ 下多個資料夾並存。這樣版本字串同時是資料夾名、頁面顯示版本，沒有映射關係要維護。

目前版本記在 `assets/_current_version.txt`，由 `menu.py` 在發布時寫入；prompt_version() 沿用或允許覆蓋。

### 路徑集中：paths.py

所有腳本的路徑一律從 `scripts/paths.py` 取，不各自硬編。未來若版面再變動，只改這一個檔。

### 領域時間結構化：領域時間.json

`extract_school.py` 新增 `--ryu-only` 參數，可以只重產領域時間而不動 CSV。領域時間另存 `versions/<版本>/領域時間.json`（結構化），供 `audit_categories.py` 的「9. 各科領域時間」一節讀取。過去只輸出 `全校_領域時間.md`（人讀用），現在同時有機器可讀的 JSON 格式。

### 退役自然科版與舊生成器

下列腳本已移入 `scripts/_legacy/` 封存，不再主動維護：

| 腳本 | 退役原因 |
|------|----------|
| `build_web_school.py` | 整頁生成器，UI / 資料耦合；由 build_data + build_single 替代 |
| `build_web.py` | 自然科版流程，停止維護；全校版工具集已取代其角色 |
| `_gen_sample.py` | 範例 HTML 注入（依賴 build_web_school 的 HTML 結構，隨整頁生成器退役） |
| `_inject_tour.py` | 新手引導注入（同上，整頁生成器退役後無用） |

`extract_v2.py` 雖舊但保留：`extract_school.py` 仍 import 其 `parse_cell`、`extract_teacher_header` 等解析函式，移除會破壞 extract 流程。

`natural_science/` 資料夾（自然科 CSV / MD / HTML）停止維護，維持現有狀態，不進一步更新。

### 母檔保護與修復（template/ + 選單 [6]）

UI 母檔移到專屬的 `template/代課查詢_發布.html`（加 `請勿刪除_說明.md`），讓「唯一 UI 真相來源」的角色一目了然、不易誤刪。

新增選單 [6] 修復母檔：母檔不見時自動重建——優先從 `live/index.html`（母檔的逐字複本）還原；若 live 也沒了，從最新的 `versions/*/代課查詢_單檔.html` 反向重建（把內嵌資料區塊換回 `<script src="data.js">`）。選單開機時若偵測母檔或 live/index.html 不存在會顯示提示。不復活已退役的整頁生成器，維持單一 UI 來源。

## 階段十三：介面微調 + 連堂代/調課補強（2026-06）

針對 `template/代課查詢_發布v2.html` 的一批 UI/UX 微調與連堂邏輯補強。分兩階段交付，各自驗證後再進下一步：**Phase 1＝純呈現（HTML/CSS/i18n，不碰配對與代/調行為）**，**Phase 2＝邏輯／資料（候選分組、調課、連堂語意）**。動機是降低風險——低風險的目視改動先落地，邏輯改動單獨回歸測試。

### Phase 1：呈現層

- **IB 模式勾選框移到老師下拉左邊**：IB 模式會改變下拉選單內容，放在前面（先選模式再選人）比放在下方合理。
- **語言切換改分段鈕**：原本是「顯示另一語言」的單顆按鈕（中文時顯示 `EN`），語意像下拉、不直覺。改成 `中｜EN` 分段控制、現用語言高亮——切換是 toggle 不是選單。
- **ⓘ 說明移到標題右側同行**（`.sec-head` flex 包住 h2 + 說明鈕）。
- **選人選後降噪**：選了候選人（`partner-mode`）後，把標題、說明引導整行、類別標題、未選人選鈕一律轉灰，只留「選中的橘色鈕」與「要調課／要代課」字樣醒目，降低認知負荷、引導下一步。曾嘗試用 `:has()` 只保留「選中人選所在類別」的標題不降噪，但 `:has()` 在目標環境沒生效（不支援時整條規則被丟棄），且需求方傾向簡單——最後回到「全部類別標題一律降噪」的純 CSS 版本。教訓：呈現層優先選最簡實作。

### Phase 2：連堂與藍框

**連堂偵測：維持白名單，不全面通用化。** 連堂判斷寫在網頁端 `isInquiryCourse()`（白名單），不在 data。原本只認三種探究課（物理地科探究／化學生物探究／IPSS），漏掉了 A23 全為相鄰雙節的 生科／領域課程。考慮過「任何相鄰同班雙節都當連堂」的通用化，但否決：**資料上相鄰雙節 ≠ 必須連續授課**（例：選修物理相鄰兩節但可拆開調課）。改為產出 `連堂課程_待確認清單.md`（61 種相鄰雙節課程＋任課老師），逐課跟老師確認後才加白名單；本次先列入 生科、領域課程。前提澄清：**在白名單是非破壞性的**——連堂課仍可代一節／調這節／調另一節，白名單只是「額外」多出「兩節都能代/調」群組與「建議同一位老師」提示。

**連堂代/調課：清單決定動作，不堆按鈕。** 一度設計成選人後給多顆動作鈕（代兩節／只代一節／調兩節／調這節），但會擠出 4 顆鈕、認知負荷高。改採需求方提的模型：**人選清單本身分組**——第一優先「兩節都能代/調」、第二優先「只能這節代/調」，兩清單互斥；選哪個清單，確認鈕（代兩節 vs 代這節）自然對應，永遠只有 1 顆確認鈕＋取消。調課一律「點課表上亮起的時段」逐節完成（要兩節都調＝分兩次點），不另設按鈕。協助老師的回補時段不需要也是連堂。

**雙重指派防呆。** 逐節處理連堂時踩到的 bug：先單獨處理連堂其中一節後，再點另一節仍會被 `getPairedPeriod()` 配對回已處理的那節，導致重複指派。修正集中在 `getPairedPeriod()`：算出配對節後，若該節已在 `state.confirmed` 就回 `null`（視為單節）——一處修好同時修正 `getCandidateGroups`、`inquiryNote`、`partnerCoversBoth`、`confirmSub` 四個下游。

**藍框「只能代課」課程。** 多元／自主／行政會報這類課不能調課、只能代課，用**藍框**標示（與連堂紫框同明度的藍 `#3b7fed`，但屬不同機制）。以課名關鍵字判斷（`isSubOnlyCourse`），三張課表（主課表、自己截圖、對方截圖）都加 `sub-only` class，並在 `isSwapOption` 對這類課直接 `return false`，面板加藍色 `panel-note` 與圖例 chip。藍框優先於紫框（「不能調課」最關鍵）。對方截圖表依「該格實際顯示的課程」決定要不要藍框（代我課的格看我的課，其餘看對方的課）。

> 改動全在母檔 `template/代課查詢_發布v2.html` 一檔內（CSS／i18n 中英／body／JS），外加 `連堂課程_待確認清單.md`。data.js、html2canvas 不動。

## 階段十四：三角調 + 教務處整理（2026-06）

`template/代課查詢_發布v2.html` 新增「三角調」：A 想指定一位**本節有課**的 B 幫忙代課，系統先替 B 把這節的課找 C 處理（代或調），B 才空出來代 A。第一版只做一層 `A ← B、B ← C`，不做四角以上。

### 核心架構：focusCode() 焦點老師間接層

關鍵洞察——**B←C 這一段在結構上和既有的 A←partner 完全相同，只是主角從 A 換成 B**。所以不另寫一套引擎，而是把所有「正在被處理的人」的依賴從硬寫 `meCode()` 改成可切換的 `focusCode()`：一般情況回 A，三角調進行中回 B。`getCandidateGroups`／`isSwapOption`／`computeCellState`／`renderPanel`／主格子渲染全部沿用，替 B 找 C 直接重用同一套候選分組、調課判斷與連堂/藍框限制。新增的真正只有：`state.triangle`、`focusCode()`、以及把 `findConfirmed` 改成 **focus-aware**（B 焦點時忽略 A 既有的代/調課，避免畫到 B 課表上）。

三角調記錄是獨立型別 `{type:"triangle", slot, aCode, bCode, cCode, legType, swapDay, swapPeriod}`，單筆即可推導所有格子；不硬塞進既有 confirmed 的 per-partner 結構（B 在 S 的格子語意特殊）。

### UI 取捨：雙課表並排（非主課表切換）

第一版讓選了 B 後「主課表整個切換成 B」，但需求方回饋「會搞不清楚現在在調誰」。改為**雙課表並排**：A 課表永遠保留在左（唯讀脈絡、標出處理中的那節），B 課表滑入右側並接管互動。`render()` 拆出 `buildFocusGrid()`（focus 視角互動表）與 `buildAContextGrid()`（A 唯讀脈絡表），用 flex row 並排。教訓：多方關係的可理解性 > 版面精簡，寧可多一張表也要讓人一眼看出「在幫誰、處理哪一節」。

### B 樞紐格：上下分割兩色塊

B 在 S 同時「代 A」又「自己的課由 C 接走」，單格只寫「代 A」會誤會。改成上下分割：上半綠＝去代（代 A）、下半橘＝不用去（原課由 C 代）。踩到的 bug：`cell-confirmed-*` 的底色 CSS 只作用於 `td`，但分割半格是 `div` → 整個沒上到色。改用語意 class `.tri-go`（綠·去代）／`.tri-off`（橘·不用去），「綠＝去代、橘＝不用去」這條規則同時用於分割格與教務處矩陣。

### 教務處參考（獨立第 5 區）

供教學/教務組長出調代課通知單，刻意做成兩種形式並存、且自成一區（與三角調卡片分離，卡片截圖不含此區）：

- **純文字清單**：把所有已確認（一般代/調＋三角調）拆成「每列只有兩位老師」的原子事件——`代課`＝X 代 Y、`調課`＝X⇄Y。三角調自動拆成 `B 代 A`＋`C 代 B`（或 `B⇄C`），組長永遠看兩兩配對、不用懂三角。每列前加可勾 `☐`，欄位以**顯示寬度**對齊（中日韓字算 2 格，配合等寬 CJK 字型如 MingLiU）。
- **異動總表（節次×三師矩陣）**：黑框、可整區一次截圖。列＝A/B/C，欄＝受影響節次；A 請假格用灰底＋「（請假）」（被動者不佔綠/橘），B 樞紐格沿用上下分割。

### 協同（雙師）排除：班級 → 課名

三角調的 B 選單列「該節沒空的老師」，但**雙師協同**（探究／IPSS）那位和你同上一堂，列出他無意義。`coTeacherCodesAt()` 動態（非預建名單）找同節協同者並排除。第一版用「同班級同節次」判定，卻漏掉 IPSS——查 N68 週三第3節才發現 IPSS 是**併班協同、各掛不同班級代碼**（N68=116A、N60=115A、N69=114A）。改為：**探究類課程用「同課名＋同節次」**判定同一協同大區塊，一般雙師仍用「同班級＋同節次」。方向上寧可多排除不可漏排除。仍是推斷規則（同課名同節＝同一塊），若日後要 100% 精準可在 data 產生階段標 `block_id`。

### 其他

- `resetState()` 補清 `state.triangle`（原本「清除全部」漏清，三角調進行中按清除會留懸空 focus）。
- 標題列的 ⓘ說明 與 中/EN 切換縮到與「資料版本」同級（muted/slate），不搶版面；章節說明鈕不變。
- 資料版本顯示是讀 `window.__DATA_VERSION__`（data.js）**動態連動**、未寫死；bat/menu 更新版本即自動反映。

### 自動化測試

新增 `tests/triangle.test.js`（node，無外部相依；在 VM 沙箱載入母檔內嵌 script＋真實 data.js）。涵蓋：一般代/調回歸、三角調代課腿/調課腿、focus 隔離、復原、resetState 清 triangle、**協同排除（動態探究案例＋具名 N68 IPSS＋一般雙師＋同節異課不誤排）**、EN 無 undefined。`node tests/triangle.test.js`，27 項全過。需本機有未進版控的 `template/data.js`、`en_name.js`。

### 評估（未實作）

- **多角調（四角以上）**：非指數，資料模型改成 chain 陣列＋focus 堆疊即可線性泛化；但人類可理解性才是上限，且自動推薦鏈會變指數（圖上找路徑）。第一版只開到三角。
- **批次清空（組長一次清多位老師某時段）**：本質是二分配對，多項式可解。建議「代課優先自動填、少數剩下人工」——代課批次互不連動好自動；調課批次有第二層約束、易連鎖、難驗證。公平性用「本週被指派次數」當權重平攤即可。（另開規劃。）

> 改動全在母檔 `template/代課查詢_發布v2.html` 一檔內（CSS／i18n／body／JS），外加 `tests/triangle.test.js`。data.js、html2canvas 不動。

### 補述：同班跨科老師「僅可代課」未顯示（2026-06-29 追加修正）

`getCandidateGroups` 原本用 `canSwapMyKlass` 當**過濾器**：同班但跨科、這節剛好沒有可對調時段的老師會被整個濾掉，完全不出現在候選名單——但他其實還是可以被請代課，只是不能調課。這是一個真實的漏洞：老師會因為看不到人而以為沒人能幫忙。

修正：`canSwapMyKlass` 改回**旗標**（`subOnlyCodes`）而非過濾器，同班群組一律顯示，可調課者排前、僅能代課者排後並附灰字「僅可代課」／`SUB ONLY` 徽章；`partnerNoSwap` 文案也從被動敘述改成可操作提示（直接引導點「確認代課」）。IB／非 IB 兩條路徑同步套用。

## 階段十五：v2 母檔正式取代 v1，接進部署流程（2026-09）

### 發現的問題

`更新課表.bat` → `scripts/menu.py` 的整條發布流程（單檔 HTML／`live/` 網頁）從 `scripts/paths.py` 的 `MASTER = template/代課查詢_發布.html` 讀母檔——但這個檔名指的其實是**階段十二重構後就沒再改過邏輯**的舊版（v1，git 歷史上只有 `758f88c` 一次改動）。階段十二～十四的所有邏輯開發（雙語介面、連堂補強、三角調課全套、教務處整理，共 17 個 commit）都只寫進另一個檔案 `template/代課查詢_發布v2.html`，從未被 `paths.MASTER` 引用到。

結果：不管是雙擊 `更新課表.bat` 產出的單檔，還是 `live/` 上實際服務的網頁，用的都是舊邏輯，三角調課等功能完全沒生效——即使資料（`data.js`）持續在更新。

### v1 → v2 邏輯差異總覽（不含資料/UI 純樣式微調，見階段十二～十四細節）

以「使用時漏洞是否減少」為主軸，v2 相對 v1 新增/修正的規則：

| 類別 | v1 行為 | v2 行為 |
|------|---------|---------|
| 連堂課（探究等相鄰雙節課） | 只認 3 種白名單課程，其餘漏判 | 擴充白名單（含 A23 生科／領域課程），並產出 `連堂課程_待確認清單.md` 供逐課確認 |
| 連堂代/調課 | 無特別處理，容易漏派或誤判 | 候選清單依「兩節都能代/調」vs「僅這節能代/調」分組，`getPairedPeriod()` 修正雙重指派 bug |
| 只能代課的課（多元/自主/行政） | 無標示，容易誤點去調課 | 藍框標示 + `isSwapOption` 直接擋掉，避免誤操作 |
| 同班跨科老師（這節無可調時段） | 直接從候選名單濾掉，老師誤以為沒人能幫 | 仍顯示於同班群組，標「僅可代課」灰字徽章（見上方補述） |
| 三角調課（A←B←C） | 無此功能，B 這節有課時只能放棄找他 | 完整三角調流程：`focusCode()` 間接層重用既有配對引擎，雙課表並排 UI，B 樞紐格上下分色 |
| 協同（雙師）課程排除 | 無此概念 | `coTeacherCodesAt()` 動態排除同節協同者；IPSS 併班各掛不同班級代碼，改用「同課名＋同節次」判定 |
| 教務處資料 | 無 | 「調代課通知清單」（純文字、可勾方框）+「異動總表」（節次×三師矩陣），供組長開單用 |
| 語言 | 僅中文 | 中英雙語切換 |

詳細設計取捨見階段十二～十四；上表只列**行為/規則層級**的差異，供日後追溯「這個規則什麼時候、為什麼變的」時快速定位。

### 執行方式：改檔名而非改程式碼

`paths.py`／`menu.py`／`build_single.py` 全部認的是檔名 `template/代課查詢_發布.html`，不是特定內容。因此不改任何程式碼，只用 `git mv -f template/代課查詢_發布v2.html template/代課查詢_發布.html` 把 v2 內容覆蓋成母檔本體，v2 檔名走入歷史。驗證：`python scripts/build_single.py --version 115-1` 產出的單檔內含「三角調」字樣（grep 80 處命中），確認新母檔確實吃到 v2 邏輯。

### 如何找回 v1 舊母檔

v1 在被 v2 覆蓋前，最後一次改動是 commit **`758f88c`**（"Add menu [6] repair + protect master"），之後再無變動。因為 `git mv -f` 是覆蓋既有檔案而非單純改名，`git log --follow` 不保證能自動接上兩段歷史（v1/v2 內容差異大，相似度判定可能抓不到 rename）。要找回 v1 內容，直接用該 commit 取檔案即可：

```
git show 758f88c:template/代課查詢_發布.html > v1_舊母檔_參考用.html
```

或想看 v1 完整演進歷程（階段一～十一），對照本 DEVLOG 該幾節。

---

## 階段十六：115-1 課名改版造成的資料流失 + 課程性質規則確認（2026-09）

> 起點：使用者發現 PDF 第 8 頁明明有 A11 這位老師，網頁下拉選單卻找不到這位老師。
> 分析全文見 [issue_dataloss.md](issue_dataloss.md)，規則決議見 [課程性質_待確認清單.md](課程性質_待確認清單.md)。

### 病灶：一次改名，兩層靜默失效

115-1 學校把課名改了三組（`行政會報`→`行政會議`、`領域時間`→`藝能領域/社會領域/…`、`團體活動`→`團體活動時間`），而程式兩層都用**完整課名白名單**比對：

1. **資料層**　`classify_course_subject()` 的 flex 清單沒命中新課名 → 這些課從「不投票」變成投給「其他」。行政兼職老師課少（多半就是行政會議＋領域時間），主授科目整個翻成「其他」。
   更陰險的是**不一致**：`英文領域` 命中「英文」投給英、`數學領域` 命中「數學」投給數，其餘領域課投給「其他」——同一類課三種下場。
2. **顯示層**　`populateTeacherDropdown()` 只跑寫死的 `DROPDOWN_ORDER`（10 科白名單），`grouped["其他"]` 永遠沒機會進 `<select>`。**沒有錯誤、沒有 fallback、沒有計數檢查** → 7 位老師整個人消失，134 人只列 127 人。

### 第三次踩同一個坑：名冊從衍生資料反推

依 Q2 決議把領域時間排除出 CSV 後，**N28**（全學期只有「自然領域」兩格）在 CSV 變成零筆，而 `build_data.py` 的教師名冊是**從 CSV rows 反推**的 → 134 掉成 133。跟 A11 是同一種 bug 的第二個實例。

教訓寫成規則：**名冊要來自權威來源（PDF 每頁表頭），不能從衍生資料反推。** `extract_school.py` 因此新增 `versions/<版本>/teachers.json`，`build_data.py` / `build_single.py` 有這個檔就以它為準（缺檔則沿用舊行為，相容 114-2）。

### 修法：規則式比對 + 職稱括號 + 三道保險

| 層 | 改法 |
|------|------|
| 課名判斷 | 完整課名白名單 → 規則式：`以「領域」結尾`（排除「領域課程」）、`含「行政會」`、`含「團體活動」`。下學期再改名只要詞根不變就不用動程式 |
| 主授科目 | 新增 `POST_TAG_TO_SUBJECT`／`post_subject()`，用 PDF 職稱括號（`教師(健)`、`國際交流處主任(體)`、`教師(科技)`）判定 A/L 系。優先序：**代號 prefix ＞ 職稱括號 ＞ 課名投票**。全量比對 134 位：與原判定 124 位一致，10 位差異中 7 位正是消失的老師（全判對），2 位無科別括號自動退回課名投票 |
| 教師名冊 | 新增 `teachers.json`（來源＝PDF 表頭） |
| 保險 1 | `extract_school.py` 寫 CSV 前檢查「主授科目 ∉ 網頁科目清單」，有就逐位印出代碼/姓名/職務 |
| 保險 2 | `dropdownSubjects()`：白名單打頭，資料裡有但不在白名單的科目**一律補列在最後**並 `console.warn` |
| 保險 3 | 新增 `tests/rules.test.js`（18 項），把每條課程性質規則寫成斷言 |

### 課程性質規則（本次確認定案）

| 課別 | 資料 | 畫面 | 可調 | 可代 |
|------|------|------|:--:|:--:|
| 領域時間（XX領域） | **排除出 `__DATA__`，另出 `window.__RYU__`** | 空堂＋**灰框**＋淡色課名 | 不適用（空堂可被調進來） | 不適用 |
| 行政會議 | 保留 | ⚠ 標記 | ✅ | ✅ |
| 團體活動時間 | 保留 | **藍框**＋⚠ | ❌ | ✅ |
| 多元／自主 | 保留 | 藍框 | ❌ | ✅ |

領域時間刻意做成「資料是空堂、畫面只標示」——**規則邏輯完全不必知道有這回事**，`isOccupied()`／`isSwapOption()`／三角調一行都沒動，灰框純粹是 `buildFocusGrid()` 多掛一個 class。這比在每條規則裡加「除非是領域時間」的例外分支安全得多。

藍框判斷改成含「團體活動」而非「行政會報」：團體活動是全校固定時段（五5、五6），跟人對調物理上不成立；行政會議則確認為可調可代，改用 ⚠ 提示而非硬擋。

### 順帶：班級被塞進課名的格子

E62 有 3 格是單行 `201 Wri`（排課系統沒把班級另起一行），導致班級欄空白、同班媒合抓不到。`parse_cell()` 加上：**班級欄為空且課名以 3 位數班級代碼開頭**才拆成 課名 `Wri` / 班級 `201`。條件收得緊，全 134 頁只有這 3 格命中。

### 成果

`__DATA__` 2035 → 1819 筆（− 216 格領域時間）、`__RYU__` 217 格、教師名冊 134 位全部可選、殘留「XX領域」0 筆。測試 27 + 18 = 45 PASS。

### 補記：連堂（紫框）也整組失效了

同一次課名改版還打死了第三個功能，只是當下沒發現：`isInquiryCourse()` 的白名單（物理地科探究／化學生物探究／IPSS／生科／領域課程）在 115-1 **命中 0 筆**——學校把課名改成 `物理-探究B`、`化學-探究A`，`生科` 改成 `生活科技`，IPSS 這學期沒開。紫框、「兩節都能代/調的老師」群組、連堂提示全部靜靜地消失。

更麻煩的是 PDF 裡**同一堂協同課有兩種寫法**：有教室的那位老師是 `物理-探究B`，另一位是 `物理-探究 B`（多一個空格）。課名不統一的話，`coTeacherCodesAt()` 對連堂課是用「同課名＋同節次」比對協同者的，兩位老師會被當成不同課。

處理：
- `COURSE_RENAMES` 加上四筆空格合併（`物理-探究 B`→`物理-探究B` 等）。不用通用的「去空白」規則，因為 `英語文 B`、`SEHS SL` 這類課名的空格是有意義的。
- `isInquiryCourse()` 改成 `INQUIRY_NAMES` 白名單 + 規則 `含「-探究」`，並保留 114-2 舊課名以相容。
- 重新產出 `連堂課程_待確認清單.md`（115-1 共 76 種相鄰同班雙節，目前判定為連堂的 3 種）。

`tests/rules.test.js` 加了一條**反脫節斷言**：連堂配對實際命中數必須 > 0。這類「白名單與資料脫節」的 bug 不會拋錯、不會少資料，只會讓功能安靜地不作用——只能靠這種「規則必須真的命中資料」的測試抓。

### 頁尾「判斷規則說明」

規則講清楚了才有人敢用。把上述規則寫成不涉及程式的版本，兩個地方各放一份：
- [調代課規則說明_教學組長版.md](調代課規則說明_教學組長版.md)——給教學組長對照
- 網頁最下方的「ⓘ 判斷規則說明」摺疊區（中英雙語，內容存在 i18n 的 `rulesBody`）

該區塊在主 `<script>` 之後才存在，`init()` 裡的 `.help-btn` 綁定掃不到，所以在頁尾既有的 IIFE 裡補綁一次展開事件。

---

## 階段十七：附錄 B 課名歸科 + IB 課程改用「子科目」四階排序（2026-09）

### 附錄 B：把落到「其他」的課名歸位

階段十六留下 26 種沒被任何關鍵字命中、全投給「其他」的課名。逐一確認後套進
`classify_course_subject()` / `classify_course_detail()`：現代社會與經濟／社會環境議題／走讀臺灣→社、健康與護理／全民國防教育／SEHS SL→體、生活科技／工程設計專題／基本設計／創新生活與家庭／CS／Computer Sci／AI程式設計→藝、國文充補→國、Wri→英、ESS SL→自。

**不確定的一律維持「其他」**（臺灣手語、WG、OH、TSP、CP）。這幾門課的老師都另有課程撐住科別，不影響下拉選單，只影響「同科優先」排序的精細度——寧可留白也不要猜錯。

順帶修掉兩個同類的「課名比對抓不到」問題：`TOK Ch`／`TOK Eng`（PDF 帶空格，原本只比對無空格版本）；以及細科目判定——114-2 的「化學生物探究」橫跨兩科所以刻意不判細科，但 115-1 改名後的「化學-探究A」已指明單一學科，改回讓它決定，否則 N09 全學期只教這門課就會變成沒有細科目。

### IB 課程：優先序改用「子科目」，並標鐘點

原本 IB 課程走 5 階（IB同科 → IB同班 → IB其他科 → 同科 → 其他科），依據是**主授科目**。改成 4 階，依據換成**細科目（子科目）**：

| 階 | 群組 | 鐘點 |
|:--:|:--|:--|
| 1 | 同子科的 IB 教師 | 2 倍 |
| 2 | 同子科的老師（非 IB） | 雙語 2 倍、非雙語原鐘點 |
| 3 | 其他 IB 教師 | 原鐘點 |
| 4 | 其他老師 | 原鐘點 |

理由：IB 課程講究實際教的學科（物理科 / 體育科），主授科目那層（自 / 體）太粗。

「雙語」是新資料——PDF 職稱本來就標了（如「教師(生)+雙語」），只是以前沒抽。新增 `is_bilingual_post()`，寫進 `teachers.json` 與 `window.__TEACHERS__` 的 `isBilingual`。

鐘點標記做在候選按鈕上（紅「2倍鐘點」／灰「原鐘點」），不是只寫在說明裡——**排序決定看到誰，標記決定敢不敢按**，兩者要在同一個地方。群組帶 `rate` 屬性（`"double"` / `"byBilingual"`），`rateTagHtml()` 依老師的 `isIB` / `isBilingual` 決定顯示哪一個。

兩個設計取捨：

- **IB 課程不再另立「同班」群組。** 四階已經把所有人分完，同班老師會落在他所屬的那一階。非 IB 課程的分組完全沒動。
- **子科目空白時退回主授科目比對**（`myDetail ? (t => t.detail === myDetail) : isSame`）。少數老師（只教彈性課、或課名判不出細科）沒有細科目，不退回的話四階會前兩階全空。

同子科群組裡人人同子科，原本標「同細科」的 ★ 就成了雜訊 → 該群組把 `leaveDetail` 傳成 `null`，`candBtn` 就不畫 ★（排序不受影響，因為群組內該值是常數）。

新增 `tests/ib.test.js`（23 項）：用真實資料驗四階的分派互斥且不遺漏、鐘點對應、非 IB 課程分組不受影響、中英字串齊全。

### 修正：IB 排序改成三階，鐘點只做提醒

上一版把「雙語」當成老師的屬性（從 PDF 職稱抽 `isBilingual`），在候選按鈕上直接標「2倍鐘點／原鐘點」。這是錯的——**雙語指的是雙語授課，是這一節怎麼上，不是老師的性質**。老師的性質只分 IB 教師與普通教師。程式無從得知代課當下會不會用雙語上，標在個別老師身上等於在斷言自己不知道的事。

改法：

- 移除 `is_bilingual_post()` 與 `isBilingual`（`teachers.json`、`__TEACHERS__` 都不再有這個欄位）
- 移除候選按鈕上的鐘點徽章與 `rateTagHtml()`
- 鐘點改成**一段提醒文字**，出現在三個地方：候選面板、自己的課表、對方的課表；**代的不是 IB 課程就完全不出現**（`confsCoverIbCourse()` 判斷）

排序也從四階收成三階：**IB教師+同子科 → IB教師+其他科 → 其他老師**。原本的第二階「同子科但非 IB 教師」併回第三階——IB 身分優先於科目吻合。

最重要的一條：**這是排序，不是限制。** 前一版把候選拆成四個互斥群組時容易讓人誤以為在篩選；這次在 `tests/ib.test.js` 加了一條斷言直接盯死——候選總人數必須等於該時段所有空堂老師人數，少一個就 FAIL。「排序」與「過濾」在 UI 上長得很像，但意義完全不同，值得用測試釘住。

---

## 階段十八：判不出科別的老師，改在更新流程裡問清楚（2026-09）

階段十六補了兩道保險：`extract_school.py` 會印警告、網頁 `dropdownSubjects()` 會把白名單外的科目補列在最後。但警告只是「印出來」，滾過去就沒了；補列在最後也只是不讓人消失，科目仍是「其他」。

真正的缺口是：**判不出科別這件事，只有人知道答案，而程式從來沒問過人。**

於是把它接進 `更新課表.bat` 的流程：

- 選項 `[1]` 新學期完整更新時，抽取完先檢查名冊，有判不出科別的老師就**逐位互動詢問**，
  畫面上一併顯示他的職務與這學期的課，好讓維護者判斷
- 答案存進 `versions/<版本>/科別修正.json`，**之後自動沿用**，不用每學期重答
- 新增選項 `[7]` 可以隨時補指定（例如當初 Enter 跳過的）
- `extract_school.py` 自己跑時也讀同一份修正表，優先序最高（人工指定 > 代號 prefix > 職稱括號 > 課名投票）

一個刻意的取捨：**選項 [7] 不重跑 `extract_school.py`**。重跑會重讀 PDF、覆蓋維護者手動修過的 CSV（README 裡「改 CSV 後絕對不要再跑 extract_school」這條陷阱）。改成只改 `teachers.json` 與 CSV 的「主授科目」單一欄位，其餘原封不動。兩條路徑（[7] 直接改欄位 vs 下次跑 extract 讀修正表）結果一致。

跳過也是有效答案：不指定就維持「其他」，網頁靠 `dropdownSubjects()` 仍會把他列在選單最後，只是分在「其他」組。保險機制沒有被這個新流程取代，是疊在一起的。
